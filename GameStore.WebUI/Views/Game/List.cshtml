@using GameStore.Domain.Entities
@model IEnumerable<Game>

@{
    ViewBag.Title = "Products";
}

@foreach (var p in @Model)
{
    <div>
        <h3>@p.Name</h3>
        <p>@p.Description</p>
        <h3>@p.Price.ToString("$ #")</h3>
    </div>
}
